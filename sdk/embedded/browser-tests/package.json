{
  "name": "@typedb/embedded-browser-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:ui": "vitest --ui",
    "test:browser": "vitest run --browser.enabled --browser.headless --browser.name=chromium"
  },
  "devDependencies": {
    "@vitest/browser": "^3.2.2",
    "playwright": "^1.52.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.2"
  }
}
